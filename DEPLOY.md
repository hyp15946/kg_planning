# Vercel 배포 가이드

Next.js **정적 내보내기**다. 서버도 환경변수도 없고, 빌드 결과는 정적 파일뿐이다.
**배포 자체는 5분이고, 나머지는 구글 설정이다.**

## 순서

```
1. Vercel 에 리포 연결   ← 기본값 그대로 두면 된다
2. 배포 도메인 확인
3. 그 도메인을 배포용 OAuth 클라이언트에 등록   ← 여기까지 안 하면 로그인이 안 된다
4. 배포용 클라이언트 ID 를 lib/config.ts 에 넣고 재배포
5. 드라이브 폴더를 팀에 공유
```

3번까지 안 하면 사이트는 뜨지만 **로그인이 안 된다.**

---

## 1. Vercel 에 리포 연결

[vercel.com](https://vercel.com) → **Add New → Project** → 이 리포 선택(**Import**).

**바꿀 것이 없다.** Vercel 이 Next.js 를 알아보고 아래대로 잡는다.

| 항목 | 값 |
|---|---|
| Root Directory | **`./`** (리포 루트 — 기본값) |
| Framework Preset | `Next.js` (자동) |
| Build Command | `next build` (자동) |
| Output Directory | 자동 |
| Environment Variables | **없다** |

> `next.config.ts` 에 `output: "export"` 가 있으므로 결과물은 정적 파일이다.
> Vercel 이 서버리스 함수를 만들지 않는다. **이 설정을 지우면 서버가 생기고,
> «배포물에 지킬 데이터가 없다» 는 접근 통제의 전제가 무너진다** (`README.md` 「접근 통제」).

**Deploy** 를 누른다. 1분 안에 끝난다.

로컬에서 결과물을 미리 확인하려면:

```bash
npm run build && npx serve out -l 8000
```

## 2. 배포 도메인 확인

`https://<프로젝트명>.vercel.app` 형태의 주소가 나온다. 이 주소를 열면
**「사내 전용」 로그인 화면**이 떠야 한다. 여기까지 왔으면 배포는 끝났다.

아직 `DEPLOY_CLIENT_ID` 를 안 넣었다면 「클라이언트 ID 가 없습니다」가 떠 있는 것이 정상이다.
3번에서 도메인을 등록하고 4번에서 ID 를 넣는다.

> **Preview 배포 주소는 매번 바뀐다.** Vercel 은 커밋마다
> `<프로젝트>-<해시>-<팀>.vercel.app` 을 새로 만든다. 그 주소는 OAuth 에 등록되어 있지
> 않으므로 로그인이 안 된다 (`redirect_uri_mismatch`).
> **로그인 테스트는 항상 고정된 Production 주소에서 한다.**

사내 도메인을 붙이려면 **Settings → Domains** 에서 추가한다.
그 경우 3번에 **그 도메인도 함께** 등록해야 한다.

## 3. 배포 도메인을 배포용 OAuth 클라이언트에 등록

[console.cloud.google.com/auth/clients](https://console.cloud.google.com/auth/clients)
→ **배포용** 클라이언트 → 아래 두 개를 등록한다.

| 항목 | 값 |
|---|---|
| 승인된 JavaScript 원본 | `https://kg-planning.vercel.app` |
| 승인된 리디렉션 URI | `https://kg-planning.vercel.app/` |

**리디렉션 URI 는 끝의 `/` 까지 정확히 맞아야 한다.** 직접 타이핑하지 말고,
배포된 사이트의 **「개발자 설정」** 안에 표시된 값의 **「복사」** 버튼을 쓴다.
사이트가 자기 주소로 만든 값이므로 그것이 정답이다.

> 반영에 **5분에서 몇 시간**까지 걸린다. 바로 안 되면 기다렸다 다시 해본다.
> 급하게 여러 번 고치면 무엇이 문제였는지 알 수 없게 된다.

아직 안 했다면 `OAUTH_SETUP.md` 의 2·4·5번도 확인한다 —
**Slides API + Drive API 사용 설정**, **Audience 를 «내부»**, **스코프 4개**.

## 4. 배포용 클라이언트 ID 를 코드에 넣기

`lib/config.ts` 의 `DEPLOY_CLIENT_ID` 에 3번 클라이언트의 ID 를 넣고 커밋·푸시한다.

```ts
export const DEPLOY_CLIENT_ID = "xxxxxxxx.apps.googleusercontent.com";
```

이러면 **쓰는 사람이 아무것도 입력하지 않는다.** 열면 로그인 버튼만 있다.

> ❌ **그 클라이언트에 `localhost` 를 추가하지 않는다.** ID 가 공개 리포에 올라가므로,
> localhost 가 등록되어 있으면 남의 PC 로컬 페이지가 우리 ID 로 드라이브 읽기 토큰을
> 받아낼 수 있다. 로컬 개발은 **별도 클라이언트**를 쓴다 — `OAUTH_SETUP.md` 6·7번.

## 5. 드라이브 폴더를 팀에 공유

`개발 볼륨 산출 데이터` 폴더를 쓸 사람들에게 공유한다 (**보기 권한이면 충분**).

**공유되지 않은 계정은 로그인해도 데이터를 못 받는다.** 그것이 의도된 동작이다 —
접근 통제를 드라이브 권한이 하고 있다. 자세한 내용은 `README.md` 의 「접근 통제」.

여기까지 하면 쓰는 사람은 **배포 주소를 열고 로그인 버튼만 누르면 된다.**
입력할 것이 없다.

---

## 배포 뒤 확인할 것

| 확인 | 정상 |
|---|---|
| 배포 주소를 연다 | 「사내 전용」 로그인 화면 + 로그인 버튼이 눌리는 상태 |
| 「개발자 설정」을 연다 | 리디렉션 URI 가 `https://kg-planning.vercel.app/` 로 표시 |
| 사내 계정으로 로그인 | 산출 화면 진입 + 헤더에 본인 이메일 |
| 「데이터」를 연다 | `드라이브 — projects.json · 폴더 「개발 볼륨 산출 데이터」 · 수정 …` |
| 사외 계정으로 로그인 | 화면이 잠기고 「@kiglestudio.com 계정으로 로그인하세요」 |

## 데이터를 갱신할 때

**배포를 다시 하지 않는다.** 드라이브의 `projects.json` 을 덮어쓰면 끝이다.
데이터는 탭을 닫으면 사라지고 다시 열 때 드라이브에서 새로 받으므로,
사용자는 다음 접속에 최신값을 본다.

지금 열려 있는 탭에서 바로 확인하려면 **「데이터」 → 「드라이브에서 다시 불러오기」**.

## 안 될 때

| 증상 | 원인 |
|---|---|
| **빌드 실패** | 로컬에서 `npm run build` 로 먼저 재현한다. 대개 타입 오류다 |
| `redirect_uri_mismatch` | 배포 도메인을 등록하지 않았거나, Preview 주소로 접속했다 (2·3번) |
| `origin_mismatch` | 승인된 JavaScript 원본을 안 넣었다 (3번) |
| `access_denied` | Audience 가 「외부」다. 「내부」로 바꾼다 |
| 로그인은 되는데 **「기준 데이터 없음」** | 폴더가 그 계정에 공유되지 않았다 (4번), 또는 Drive API 미설정 |
| **「권한 범위가 부족합니다」** | 스코프를 늘리기 전에 받은 토큰이다. 「연결 해제」 후 다시 로그인 |
| 맞게 했는데도 안 된다 | 구글 설정 반영에 최대 몇 시간. 기다렸다 재시도 |

나머지 오류는 `OAUTH_SETUP.md` 의 「자주 나는 오류」에 정리되어 있다.

## 로컬에서 확인하려면

```bash
npm run dev        # http://localhost:8000
```

**여기서는 로그인이 안 된다.** 배포용 클라이언트에 `localhost` 가 등록되어 있지 않기
때문이고, 그게 의도다 (4번). 로컬에서 로그인까지 확인해야 하면 **로컬 개발용 클라이언트**를
따로 만들어 첫 화면의 **「개발자 설정」** 에 그 ID 를 넣는다 — `OAUTH_SETUP.md` 6-2·7번.

로그인이 필요 없는 화면 작업이라면 「드라이브를 쓸 수 없을 때」로 `projects.json` 을
직접 넣어 확인하는 편이 빠르다.

`localhost` 는 HTTPS 가 아니어도 되지만,
**배포 도메인은 반드시 HTTPS** 여야 한다 (Vercel 은 자동으로 HTTPS다).
